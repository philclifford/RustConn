{
  "version": "v1",
  "hooks": [
    {
      "name": "KiroGraph Mark Dirty on Create",
      "description": "Mark the KiroGraph index as dirty when source files are created.",
      "trigger": "PostFileCreate",
      "matcher": "\\.(rs|toml)$",
      "action": {
        "type": "command",
        "command": "cd \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\" || exit 0; kirograph mark-dirty >>.kirograph/hook.log 2>&1 || true"
      },
      "enabled": true
    }
  ]
}
